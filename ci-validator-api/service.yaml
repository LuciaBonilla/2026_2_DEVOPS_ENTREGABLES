apiVersion: v1
kind: Service
metadata:
  name: ci-validator-api
spec:
  type: NodePort
  selector:
    app: ci-validator-api
    version: blue
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30000